export class Ballot {
  code: string;
  name: string;
  e: number;
  n: string;
  regex_voter: string;
  regex_candidate: string;
  phase: string;
}

export class Token {
	jwt_token: string;
	name: string;
	picture: string;
}

